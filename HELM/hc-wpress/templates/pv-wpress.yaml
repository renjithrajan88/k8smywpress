apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ printf "pv-%s" .Values.wpressDeployment.name }}
  annotations:
    "helm.sh/hook": "pre-install"
    "helm.sh/hook-weight": "1"
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  storageClassName: {{ printf "stgcls-%s" .Values.wpressDeployment.name }}
  nfs:
    {{- include "nfswpress.details" . | nindent 4 }}